ctpval := [*
    <
        23,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        47,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        71,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        167,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        191,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        239,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        263,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        311,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        359,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        383,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        431,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        479,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        503,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        599,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        647,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        719,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        743,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        839,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        863,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        887,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        911,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        983,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        1031,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        1103,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >

,
    <
        1367,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        1439,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        1487,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        1511,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        1559,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        1583,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        1607,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        1823,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        1847,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        1871,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        2039,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        2063,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        2087,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        2111,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        2207,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        2351,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        2399,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        2423,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        2447,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        2543,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        2591,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        2663,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        2687,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        2711,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,
<
        2879,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        2903,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        2927,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        2999,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        3023,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        3119,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        3167,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        3191,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        3359,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        3407,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        3527,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        3623,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        3671,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        3719,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        3767,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        3863,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        3911,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        4007,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >
,
<
        4079,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        4127,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        4271,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        4391,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        4463,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        4583,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        4679,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        4703,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        4751,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        4799,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        4871,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        4919,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >,

    <
        4943,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, 1, 1, 1 ])
    >,

    <
        4967,

        MatrixAlgebra(IntegerRing(), 2) ! Matrix(2, 2, \[ 1, -1, -1, 1 ])
    >
*];
